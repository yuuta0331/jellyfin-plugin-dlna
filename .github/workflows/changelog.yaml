name: '📝 Create/Update Release Draft & Release Bump PR'

# Disabled for this fork: releases are managed manually via build.yaml and release.yaml.
on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  call:
    if: false
    uses: jellyfin/jellyfin-meta-plugins/.github/workflows/changelog.yaml@master
    with:
      repository-name: yuuta0331/jellyfin-plugin-dlna
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
